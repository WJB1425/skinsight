# ── Supabase（「我的护肤台」账号系统 / 产品库云端存储）────────────────────
# 1. 到 https://supabase.com 新建一个免费项目
# 2. Project Settings → API 里复制 Project URL 和 anon public key
# 3. 复制本文件为 .env.local，把下面两个值填进去（.env.local 已被 .gitignore 忽略）
# 4. 在 Supabase 的 SQL Editor 里执行 db/schema.sql 建表 + 开启行级安全
# 5. 部署到 Vercel 时，在 Project → Settings → Environment Variables 同样加这两个
#
# 不填也能跑：未配置时网站自动进入「本机试用模式」——账号系统禁用，
# 护肤台数据仅存在当前浏览器，不会云端同步。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ── AI 按名查成分（/api/lookup-ingredients）─────────────────────────────────
# 服务端调用，密钥不会进前端（注意：不带 NEXT_PUBLIC_ 前缀）。
# OpenAI 兼容协议，可插拔——换厂商只改这三个变量。
#
# 默认对接通义千问 Qwen（阿里云百炼）。去 https://bailian.console.aliyun.com 拿 key。
#   LLM_API_KEY   = 你的 key（百炼 / DashScope）
#   LLM_BASE_URL  = https://dashscope.aliyuncs.com/compatible-mode/v1   （海外站用 https://dashscope-intl.aliyuncs.com/compatible-mode/v1）
#   LLM_MODEL     = qwen-plus      （纯文本「按名查」用）
#   LLM_VISION_MODEL = qwen-vl-max （拍图识别：成分表 OCR / 包装识别产品 用，视觉模型）
#
# 换别家（同为 OpenAI 兼容）示例：
#   DeepSeek: BASE_URL=https://api.deepseek.com  MODEL=deepseek-chat
#   智谱GLM:  BASE_URL=https://open.bigmodel.cn/api/paas/v4  MODEL=glm-4-plus
#   OpenAI:   BASE_URL=https://api.openai.com/v1  MODEL=gpt-4o-mini
# 不填则「AI 按名查」自动禁用并提示，不影响其它功能。
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
